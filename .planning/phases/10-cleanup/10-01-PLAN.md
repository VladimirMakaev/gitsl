---
phase: 10-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - .planning/INTEGRATION.md
  - .planning/MILESTONES.md
  - .planning/milestones/v1.0-REQUIREMENTS.md
  - .planning/milestones/v1.0-ROADMAP.md
  - .planning/milestones/v1.0-MILESTONE-AUDIT.md
  - .planning/phases/01-script-skeleton/01-01-PLAN.md
  - .planning/phases/01-script-skeleton/01-VERIFICATION.md
  - .planning/phases/02-e2e-test-infrastructure/02-01-PLAN.md
  - .planning/phases/02-e2e-test-infrastructure/02-02-PLAN.md
  - .planning/phases/02-e2e-test-infrastructure/02-VERIFICATION.md
  - .planning/phases/03-execution-pipeline/03-01-PLAN.md
  - .planning/phases/03-execution-pipeline/03-02-PLAN.md
  - .planning/phases/03-execution-pipeline/03-VERIFICATION.md
  - .planning/phases/04-direct-command-mappings/04-01-PLAN.md
  - .planning/phases/04-direct-command-mappings/04-02-PLAN.md
  - .planning/phases/04-direct-command-mappings/04-VERIFICATION.md
  - .planning/phases/05-file-operation-commands/05-01-PLAN.md
  - .planning/phases/05-file-operation-commands/05-VERIFICATION.md
  - .planning/phases/06-status-output-emulation/06-01-PLAN.md
  - .planning/phases/06-status-output-emulation/06-RESEARCH.md
  - .planning/phases/06-status-output-emulation/06-VERIFICATION.md
  - .planning/phases/07-log-output-emulation/07-01-PLAN.md
  - .planning/phases/07-log-output-emulation/07-VERIFICATION.md
  - .planning/phases/08-add-u-emulation/08-01-PLAN.md
  - .planning/phases/08-add-u-emulation/08-VERIFICATION.md
  - .planning/phases/09-unsupported-command-handling/09-01-PLAN.md
  - .planning/phases/09-unsupported-command-handling/09-01-SUMMARY.md
  - .planning/phases/09-unsupported-command-handling/09-RESEARCH.md
  - .planning/phases/09-unsupported-command-handling/09-VERIFICATION.md
autonomous: true

must_haves:
  truths:
    - "No references to 'get-shit-done' remain in planning documentation"
    - "No references to '@~/.claude/get-shit-done/' remain in any file"
    - "No references to 'gsd-verifier' or 'gsd-integration' remain in any file"
    - "No references to '/gsd:' commands remain in any file"
    - "Python source files remain unchanged (already clean)"
  artifacts:
    - path: ".planning/phases/01-script-skeleton/01-01-PLAN.md"
      provides: "Cleaned PLAN.md without workflow references"
      contains: "No @~/.claude/get-shit-done lines"
    - path: ".planning/phases/01-script-skeleton/01-VERIFICATION.md"
      provides: "Cleaned VERIFICATION.md with generic verifier attribution"
      contains: "Claude verifier"
    - path: ".planning/INTEGRATION.md"
      provides: "Cleaned integration report with generic workflow name"
      contains: "Git Workflow"
  key_links:
    - from: "All PLAN.md files"
      to: "Execution context"
      via: "Removed external workflow references"
      pattern: "No @~/.claude/ lines"
    - from: "All VERIFICATION.md files"
      to: "Verifier attribution"
      via: "Generic Claude verifier"
      pattern: "Claude verifier"
---

<objective>
Remove all external tool references from planning documentation to make the codebase self-contained.

Purpose: The planning documentation contains 83+ references to external tools (get-shit-done, gsd-verifier, etc.) that are not part of this project. Cleaning these makes the repository self-contained and removes coupling to external systems.

Output: All planning documentation files cleaned of external tool references while preserving historical context and meaning.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-cleanup/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove external workflow references from PLAN.md files</name>
  <files>
    .planning/phases/01-script-skeleton/01-01-PLAN.md
    .planning/phases/02-e2e-test-infrastructure/02-01-PLAN.md
    .planning/phases/02-e2e-test-infrastructure/02-02-PLAN.md
    .planning/phases/03-execution-pipeline/03-01-PLAN.md
    .planning/phases/03-execution-pipeline/03-02-PLAN.md
    .planning/phases/04-direct-command-mappings/04-01-PLAN.md
    .planning/phases/04-direct-command-mappings/04-02-PLAN.md
    .planning/phases/05-file-operation-commands/05-01-PLAN.md
    .planning/phases/06-status-output-emulation/06-01-PLAN.md
    .planning/phases/07-log-output-emulation/07-01-PLAN.md
    .planning/phases/08-add-u-emulation/08-01-PLAN.md
    .planning/phases/09-unsupported-command-handling/09-01-PLAN.md
  </files>
  <action>
For each PLAN.md file listed above:

1. Read the file content
2. Delete the entire `<execution_context>` section containing:
   ```
   <execution_context>
   @~/.claude/get-shit-done/workflows/execute-plan.md
   @~/.claude/get-shit-done/templates/summary.md
   </execution_context>
   ```
3. If the file contains other "get-shit-done" references in prose (e.g., in 06-01, 07-01, 08-01, 09-01), replace them with generic terms:
   - "get-shit-done workflow" -> "git workflow"
   - "get-shit-done integration" -> "git client integration"
   - "get-shit-done uses" -> "git clients use"
4. Write the cleaned file

All 12 PLAN.md files contain the `<execution_context>` section. Some also have prose references that need updating.

Specific files with prose references to update:
- 06-01-PLAN.md: Contains "get-shit-done" in context about porcelain format usage
- 07-01-PLAN.md: Contains "get-shit-done" in context about log format usage
- 08-01-PLAN.md: Contains "get-shit-done" in context about add -u usage
- 09-01-PLAN.md: Contains "get-shit-done" in context about unsupported command handling
  </action>
  <verify>
Run verification grep commands:

```bash
# Verify no @~/.claude references remain
grep -r "@~/.claude/get-shit-done" .planning/phases/*/0*-PLAN.md && echo "FAIL" || echo "PASS: No workflow refs"

# Verify no get-shit-done text remains
grep -ri "get-shit-done" .planning/phases/*/0*-PLAN.md && echo "FAIL" || echo "PASS: No tool refs"
```

Both checks should show PASS.
  </verify>
  <done>
All 12 PLAN.md files in phases 01-09 have execution_context sections removed and any prose references to "get-shit-done" replaced with generic terms.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clean verifier attributions and prose references in remaining files</name>
  <files>
    .planning/STATE.md
    .planning/INTEGRATION.md
    .planning/MILESTONES.md
    .planning/milestones/v1.0-REQUIREMENTS.md
    .planning/milestones/v1.0-ROADMAP.md
    .planning/milestones/v1.0-MILESTONE-AUDIT.md
    .planning/phases/01-script-skeleton/01-VERIFICATION.md
    .planning/phases/02-e2e-test-infrastructure/02-VERIFICATION.md
    .planning/phases/03-execution-pipeline/03-VERIFICATION.md
    .planning/phases/04-direct-command-mappings/04-VERIFICATION.md
    .planning/phases/05-file-operation-commands/05-VERIFICATION.md
    .planning/phases/06-status-output-emulation/06-RESEARCH.md
    .planning/phases/06-status-output-emulation/06-VERIFICATION.md
    .planning/phases/07-log-output-emulation/07-VERIFICATION.md
    .planning/phases/08-add-u-emulation/08-VERIFICATION.md
    .planning/phases/09-unsupported-command-handling/09-01-SUMMARY.md
    .planning/phases/09-unsupported-command-handling/09-RESEARCH.md
    .planning/phases/09-unsupported-command-handling/09-VERIFICATION.md
  </files>
  <action>
For each file listed above, apply the following replacements:

**VERIFICATION.md files (9 files):**
1. Replace `(gsd-verifier)` with `(Claude verifier)`
2. Replace `*Verifier: Claude (gsd-verifier)*` with `*Verifier: Claude*`

**INTEGRATION.md:**
1. Replace "GSD Workflow" with "Git Workflow"
2. Replace "GSD workflow" with "git workflow"
3. Replace "Flow 1: GSD Workflow" with "Flow 1: Git Workflow"
4. Replace "get-shit-done" with "git clients"

**MILESTONES.md:**
1. Replace any "get-shit-done" with "git workflow tools"

**STATE.md:**
1. Remove or update the line `Resume with: /gsd:plan-phase 10` - change to `Resume with: Plan phase 10`

**v1.0-REQUIREMENTS.md:**
1. Replace "get-shit-done" in scope descriptions with "git workflow tools"
2. Replace "GSD" abbreviation with "workflow" or "client" as appropriate
3. Line 14: "Git commands used by get-shit-done execute correctly" -> "Git commands execute correctly"
4. Lines mentioning "GSD commands" -> "git commands"

**v1.0-ROADMAP.md:**
1. Replace "get-shit-done" references with generic terms

**v1.0-MILESTONE-AUDIT.md:**
1. Replace "GSD Workflow" with "Git Workflow"
2. Replace "gsd-integration-checker" with "integration-checker"

**06-RESEARCH.md and 09-RESEARCH.md:**
1. Replace "get-shit-done" with "git workflow tools" or "git clients" as contextually appropriate

**09-01-SUMMARY.md:**
1. Replace "get-shit-done" references with generic terms
  </action>
  <verify>
Run comprehensive verification:

```bash
# Verify no external tool references remain anywhere in .planning/
echo "=== Checking for remaining references ==="

echo "get-shit-done:"
grep -ri "get-shit-done" .planning/ --include="*.md" | grep -v "10-RESEARCH.md" && echo "FOUND" || echo "PASS"

echo "gsd-verifier:"
grep -ri "gsd-verifier" .planning/ --include="*.md" | grep -v "10-RESEARCH.md" && echo "FOUND" || echo "PASS"

echo "gsd-integration:"
grep -ri "gsd-integration" .planning/ --include="*.md" | grep -v "10-RESEARCH.md" && echo "FOUND" || echo "PASS"

echo "@~/.claude/get-shit-done:"
grep -r "@~/.claude/get-shit-done" .planning/ --include="*.md" | grep -v "10-RESEARCH.md" && echo "FOUND" || echo "PASS"

echo "/gsd: commands:"
grep -r "/gsd:" .planning/ --include="*.md" | grep -v "10-RESEARCH.md" && echo "FOUND" || echo "PASS"

echo ""
echo "=== Verify Python source files still clean ==="
grep -ri "get-shit-done\|gsd\|glittercowboy" *.py tests/*.py tests/**/*.py 2>/dev/null && echo "FOUND - UNEXPECTED" || echo "PASS: Python files clean"
```

All checks should show PASS. The 10-RESEARCH.md file is excluded as it documents what was cleaned (it's the research document for this phase).
  </verify>
  <done>
All VERIFICATION.md files have generic verifier attribution. All prose files have external tool references replaced with generic terms. No references to get-shit-done, gsd-verifier, gsd-integration, or /gsd: commands remain in planning documentation (except the research document that describes the cleanup).
  </done>
</task>

</tasks>

<verification>
Complete verification sequence:

```bash
cd /Users/vmakaev/NonWork/gitsl

echo "=== Phase 10 Cleanup Verification ==="

# 1. No get-shit-done references (excluding research doc)
echo "1. Checking get-shit-done references..."
FOUND=$(grep -ri "get-shit-done" .planning/ --include="*.md" | grep -v "10-RESEARCH.md" | wc -l)
test $FOUND -eq 0 && echo "   PASS: No get-shit-done references" || echo "   FAIL: $FOUND references found"

# 2. No @~/.claude workflow references
echo "2. Checking workflow path references..."
FOUND=$(grep -r "@~/.claude/get-shit-done" .planning/ --include="*.md" | grep -v "10-RESEARCH.md" | wc -l)
test $FOUND -eq 0 && echo "   PASS: No workflow path references" || echo "   FAIL: $FOUND references found"

# 3. No gsd-verifier references
echo "3. Checking gsd-verifier references..."
FOUND=$(grep -ri "gsd-verifier" .planning/ --include="*.md" | grep -v "10-RESEARCH.md" | wc -l)
test $FOUND -eq 0 && echo "   PASS: No gsd-verifier references" || echo "   FAIL: $FOUND references found"

# 4. No gsd-integration references
echo "4. Checking gsd-integration references..."
FOUND=$(grep -ri "gsd-integration" .planning/ --include="*.md" | grep -v "10-RESEARCH.md" | wc -l)
test $FOUND -eq 0 && echo "   PASS: No gsd-integration references" || echo "   FAIL: $FOUND references found"

# 5. No /gsd: command references
echo "5. Checking /gsd: command references..."
FOUND=$(grep -r "/gsd:" .planning/ --include="*.md" | grep -v "10-RESEARCH.md" | wc -l)
test $FOUND -eq 0 && echo "   PASS: No /gsd: command references" || echo "   FAIL: $FOUND references found"

# 6. Python files still clean
echo "6. Checking Python source files..."
FOUND=$(grep -ri "get-shit-done\|gsd\|glittercowboy" *.py tests/*.py tests/**/*.py 2>/dev/null | wc -l)
test $FOUND -eq 0 && echo "   PASS: Python files clean" || echo "   FAIL: $FOUND references found"

echo ""
echo "=== Summary ==="
echo "If all checks show PASS, Phase 10 cleanup is complete."
```
</verification>

<success_criteria>
1. No references to "get-shit-done" remain in planning documentation (except 10-RESEARCH.md)
2. No references to "@~/.claude/get-shit-done/" workflow paths remain
3. No references to "gsd-verifier" or "gsd-integration" remain
4. No references to "/gsd:" commands remain
5. Python source files remain unchanged (already clean per research)
6. All planning documentation is self-contained
7. Historical context and meaning preserved (replaced with generic terms, not deleted)
</success_criteria>

<output>
After completion, create `.planning/phases/10-cleanup/10-01-SUMMARY.md`
</output>
